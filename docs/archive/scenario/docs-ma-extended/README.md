# Archived: extended Project Horizon corpus (25 docs)

This corpus was prepared as a larger M&A due-diligence set but was **never wired** into the demo UI, Studio corpora, or benchmark manifests.

- **Active M&A demo** uses [`demo/scenario/docs/`](../../../demo/scenario/docs/) (5–7 docs).
- Restored only if a future demo/manifest deliberately adopts it.

Do not delete without checking publication needs; keep out of live drivers until then.
