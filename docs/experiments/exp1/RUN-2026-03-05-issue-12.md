# Issue #12 — Full protocol run (Exp1, 4 contradiction densities)

## Run attempt: 2026-03-05

**Command:** `bash scripts/run-exp1-full-protocol.sh`

**Outcome:** Failed during first scenario (c=0). Hatchery did not become ready within 30s (Node uncaught exception in swarm startup). **Context:** Run was attempted on the `non-scalar-finality` branch, which is still under development. Do not re-run the full protocol until the code is on `main` and the stack runs cleanly.

**No new result dirs** from this run.

## What “resolve” means for issue #12

- **Protocol:** Run all four contradiction densities c=0, 1, 3, 5 with 7 rounds and resolve-at=5,6,7.
- **Script:** `scripts/run-exp1-full-protocol.sh` implements this; no code change required.
- **Success criteria:** Four completed runs, each producing a result dir under `docs/experiments/exp1/results/<timestamp>/` with `convergence_history.json`, `decision_records.json`, `metadata.json`. Compare V(t) and gate satisfaction across c=0,1,3,5.
- **Close #12 when:** Full matrix has been run successfully (e.g. after starting Docker and `.env`), results are collected, and you accept that the core goal (multi-iteration convergence, trajectory shapes) is validated by the full matrix.

## How to run when the stack is up

```bash
# Ensure Docker stack is running (postgres, nats, s3, facts-worker) and .env has DATABASE_URL, NATS_URL
docker compose up -d

# Run full protocol (4 runs, ~15–20 min)
bash scripts/run-exp1-full-protocol.sh
```

Results will appear in `docs/experiments/exp1/results/` with one timestamped dir per c=0,1,3,5. Then you can close issue #12 with a comment pointing to those four result dirs and a one-line summary (e.g. “Full matrix run; all four densities completed; V(t) and gates as expected.”).
