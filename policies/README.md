# Policy evaluation

Governance and finality policy evaluation use the YAML engine and the sgrs Rust kernel only. No OPA/Rego integration.

See `governance.yaml` and `finality.yaml` at repo root, and `src/governance.ts`, `src/finalityEvaluator.ts`, and `sgrs-core` for implementation.
