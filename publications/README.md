# Publications (LaTeX + paper drafts)

| Path | Content |
|------|---------|
| [publication_1/](publication_1/) | Paper 1 (ArXiv): `swarm-governed-agents.tex`, `references.bib`, PDF, strengthening / bib QA notes |

**Build:** run LaTeX from inside the paper directory so `\bibliography{...}` resolves (e.g. `cd publications/publication_1 && latexmk -pdf swarm-governed-agents.tex`).
