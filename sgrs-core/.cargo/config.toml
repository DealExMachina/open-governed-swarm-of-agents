# On Alpine (musl), Rust looks for aarch64-linux-musl-gcc / x86_64-linux-musl-gcc
# but the system only has gcc. Point the musl targets at the system compiler.
[target.aarch64-unknown-linux-musl]
linker = "gcc"

[target.x86_64-unknown-linux-musl]
linker = "gcc"
