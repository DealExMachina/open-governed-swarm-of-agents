# NLI model evaluation artifacts

Generated by `pnpm run nli:verdicts` and `pnpm run nli:smoke`. See [docs/benchmarks/nli-model-eval.md](../docs/benchmarks/nli-model-eval.md).

- `verdicts-*.json` — raw model outputs per gold pair
- `report-*.json` — governance metrics + confidence sweep
- `lfm-head-*/` — fine-tuned checkpoints (weights gitignored)
